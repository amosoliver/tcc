<%= form_with(model: consultor_skill) do |form| %>
  <% if consultor_skill.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(consultor_skill.errors.count, "error") %> prohibited this consultor_skill from being saved:</h2>

      <ul>
        <% consultor_skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id_consultor, style: "display: block" %>
    <%= form.number_field :id_consultor %>
  </div>

  <div>
    <%= form.label :id_skill, style: "display: block" %>
    <%= form.number_field :id_skill %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
