<%= form_with(model: usuario) do |form| %>
  <% if usuario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(usuario.errors.count, "error") %> prohibited this usuario from being saved:</h2>

      <ul>
        <% usuario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :id_pessoa, style: "display: block" %>
    <%= form.number_field :id_pessoa %>
  </div>

  <div>
    <%= form.label :id_tipo_usuario, style: "display: block" %>
    <%= form.number_field :id_tipo_usuario %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
